/**
 * Some predefined delay values (in milliseconds).
 */
export declare enum Delays {
    Short = 500,
    Medium = 2000,
    Long = 5000
}
export declare function greeter(name: any): Promise<string>;
